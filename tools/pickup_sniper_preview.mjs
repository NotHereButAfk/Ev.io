import {createServer} from 'vite';import {chromium} from 'playwright';import fs from 'node:fs';
const server=await createServer({server:{host:'127.0.0.1',port:0}});await server.listen();const browser=await chromium.launch({args:['--use-gl=swiftshader','--enable-webgl','--no-sandbox','--enable-unsafe-swiftshader']});
try{const page=await browser.newPage({viewport:{width:1280,height:720}});await page.goto(server.resolvedUrls.local[0]);await page.waitForFunction(()=>window.__game?.previewCharacter?.userData.isEvCharacter,null,{timeout:120000});await page.evaluate(()=>document.querySelector('#play-btn').click());await page.waitForFunction(()=>window.__game?.state==='playing',null,{timeout:120000});await page.evaluate(async()=>{const {preloadWeaponModels,onWeaponModelsReady}=await import('/src/weapons/WeaponModels.js');await new Promise(r=>{onWeaponModelsReady(r);preloadWeaponModels();});const g=window.__game;g.weaponSystem.addMapGun('boltsniper');g.weaponSystem.switchTo(g.weaponSystem.loadout.findIndex(w=>w.id==='boltsniper'));g._menuOpen=false;for(const id of ['top-nav','nav-side','share-game','social-icons','center-play'])document.getElementById(id)?.style.setProperty('display','none');});await page.waitForTimeout(1200);fs.mkdirSync('../../outputs/pickup-sniper',{recursive:true});await page.screenshot({path:'../../outputs/pickup-sniper/sniper.png'});console.log('Sniper gameplay preview captured');}finally{await browser.close();await server.close();}process.exit(0);
